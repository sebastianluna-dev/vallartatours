// Public URL of the site, for everything that needs an absolute URL (sitemap,
// robots, canonicals and Open Graph). Locally it is overridden with
// NEXT_PUBLIC_SITE_URL; the default value is the production domain.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://vallartawknd.mx";
